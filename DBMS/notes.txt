Nested queires
    Syntax:
        select col_name
        from table_name
        where col_name (condition) (
            select col_name
            from table_name
            where col_name;
        );

SET OPERATIONS
    union : 
        Syntax:
            //removes duplicates

                select col_name from t_name_1
                union 
                select col_name from t_name_2

            //keeps duplicates

                select col_name from t_name_1
                union all
                select col_name from t_name_2

    intersect :
        Syntax:
            //removes duplicates

                select col_name from table_name_1
                intersect
                select col_name from table_name_2

            //keeps duplicates

                select col_name from t_name
                intersect all
                select col_name from table_name_2

    minus : does not exist in sql workbench
        alternative :
            
            SELECT c.Name
            FROM CSE_Students c
            WHERE NOT EXISTS (
                SELECT 1
                FROM AIML_Students a
                WHERE a.Name = c.Name
            );



        

            
















(DCL)
    Revoke and Grant
        Grant: grants privilege to users
        Syntax:
            1.Grant unique privelage:
                grant privelage
                on db_name.t_name
                to 'username'@'localhost';
            2.Grant multiple privelage:
                grant p1,p2,p3
                on db_name.t_name
                to 'username'@'localhost'

        Revoke: removes privelage
        Syntax:
            1.
            revoke privelage
            on db_name.t_name
            from 'username'@'localhost'
        

        
              
        
I WILL NOT STOP !!!